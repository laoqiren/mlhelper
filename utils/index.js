const Matrix = require('./matrix/index');
const Vector = require('./vector/index');
const fileParser = require('./fileParser/index');
const charts = require('./charts/index')

module.exports = {
    Matrix,
    Vector,
    fileParser,
    charts
}